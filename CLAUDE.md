# 音程チェッカー — このリポで作業するAIへ

- **曲を足す・曲のデータを直すときは、必ず先に [曲の足し方.md](曲の足し方.md) を読む。** 著作権の確認 → 楽譜の入手 → 書き方 → テンポ → 出す前の確かめ、の順で進める。
- 出す前は毎回: `zsh check-load.sh` で「runtime load ok」／スマホの幅で画面を撮って見る／`sw.js` の `CACHE_NAME` を1つ上げる／index.html 冒頭の版の説明に1行足す。
- これまでの直しと理由は index.html 冒頭の版の説明（v1.6〜）。設計は Vault の `06_Projects/20260920_音程チェックアプリ/設計_v0.md`。
